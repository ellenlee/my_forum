<h1>Create a new topic</h1>

<%= form_for @topic, url: topics_path do |f| %>
	<%= f.label :title, "標題" %>
	<%= f.text_field :title %>

	<%= f.label :content, "內容" %>
	<%= f.text_area :content %>

	<%= f.submit %>
<% end %>